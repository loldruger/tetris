use std::io::{self, Write, Stdout, StdoutLock};

pub struct Renderer {
    stdout: Option<Stdout>,
    buffer: [u8; 1024]
}

impl Renderer {
    
    pub fn render() {
        
    }

    pub fn flush() {
        
    }
}

